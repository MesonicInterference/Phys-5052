#ifndef KYLE // this just tells the compiler to only do the stuff here if KYLE isn't defined
#define KYLE // defining KYLE here ensures that this can't be included more than once and try
             // to redefine the same funciton (that'd make the compiler bitch at us a lot)

  double func_kyle(double x); // prototype function declared

#endif
